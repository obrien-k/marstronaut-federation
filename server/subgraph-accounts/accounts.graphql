extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0",
        import: ["@key"])

type Query {
  "Returns all accounts"
  accounts: [account]
  "Find one account based on ID"
  account(pl0xbrk: ID!): account
}

type account @key(fields: "pl0xbrk") {
  pl0xbrk: ID!,
  role: String!
}