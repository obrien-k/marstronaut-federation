extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0",
        import: ["@key"])

type Query {
  #"Retrieve users if logged in"
  #Comic(comic_number: Int!): Comic
  #"Return logged in user"
  #me
  "totally cool"
  totally: totally
}

type totally @key(fields: "id") {
  id: ID!,
  cool: Boolean
}